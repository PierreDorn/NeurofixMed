import { createServerClient } from '@/lib/supabase-server';
import { redirect } from 'next/navigation';
async function updateProfile(formData: FormData){'use server'; const supabase=await createServerClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/login'); await supabase.from('profiles').update({learning_profile:formData.get('learning_profile')}).eq('id',user.id); redirect('/dashboard');}
export default async function Perfil(){const supabase=await createServerClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/login'); const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single(); return <main className="mx-auto max-w-3xl p-6"><h1 className="text-3xl font-bold">Perfil de aprendizagem</h1><p className="mt-2 text-slate-600">Essa escolha ajusta o jeito como os conteúdos e gabaritos devem ser escritos.</p><form action={updateProfile} className="card mt-6 space-y-4 p-6">{[['tdah','TDAH: blocos curtos, checklist e foco no próximo passo'],['tea','TEA/autismo: previsibilidade, estrutura fixa e menos ambiguidade'],['dislexia','Dislexia: frases curtas, espaçamento e linguagem direta'],['padrao','Padrão: explicação tradicional']].map(([v,l])=><label key={v} className="flex gap-3 rounded-xl border p-4"><input type="radio" name="learning_profile" value={v} defaultChecked={profile?.learning_profile===v}/><span>{l}</span></label>)}<button className="rounded-xl bg-slate-900 px-4 py-3 font-semibold text-white">Salvar perfil</button></form></main>}
